import { LightningElement, api, track } from 'lwc';

export default class DynamicDetailsLwc extends LightningElement {
    
    // @api objectApiName;
    // @api recordId;
    // @api showDetails;
    // @api fldList;
    // @api fldListArray;
    /*
    if (fldList) {

        fldListArray = fldList.split(',').map((fld) => fld.trim());
        alert(fldListArray);
    }

    */

    

    
}